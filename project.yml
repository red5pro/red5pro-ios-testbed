name: WebRTCTestBed
options:
  bundleIdPrefix: com.infrared5.test.testbed

packages:
  WebRTC:
    url: https://github.com/stasel/WebRTC
    from: 140.0.0
  PubNubSDK:
    url: https://github.com/pubnub/swift
    from: 10.0.0
  Red5ProSDK:
    path: ../red5pro-ios-sdk/distribution

targets:
  WebRTCTestBed:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: WebRTCTestBed
    info:
      path: Resources/Info.plist
      properties:
        LSRequiresIPhoneOS: true
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: true
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        "UISupportedInterfaceOrientations~ipad":
          - UIInterfaceOrientationPortrait
        NSCameraUsageDescription: "This app needs camera access for video calls"
        NSMicrophoneUsageDescription: "This app needs microphone access for audio calls"
    settings:
      base:
        PRODUCT_NAME: WebRTCTestBed
        FRAMEWORK_SEARCH_PATHS: ["$(inherited)", "$(PROJECT_DIR)/Frameworks"]
    dependencies:
      - package: WebRTC
      - package: PubNubSDK
      - package: Red5ProSDK
        product: Red5WebRTCKit
      - package: Red5ProSDK
        product: Red5PubNubClient
      - sdk: AudioToolbox.framework
      - sdk: AVFoundation.framework
      - sdk: CoreMedia.framework
      - sdk: CoreAudio.framework
      - sdk: VideoToolbox.framework

schemes:
  WebRTCTestBed:
    build:
      targets:
        WebRTCTestBed: all
    run:
      config: Debug
