import SwiftUI

@main
struct SampleApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .background(Color(.systemBackground))
                .ignoresSafeArea(.all)
        }
    }
}
